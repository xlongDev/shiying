; 供应链 minimum-release-age（发布不满 1 天）校验的放行清单已写入
; pnpm-workspace.yaml 的 minimumReleaseAgeExclude（floating-ui /
; browserslist / postcss 等 10 个新发布的主流包）。
; 本文件无需策略配置，保留此说明即可。

; pnpm 11 安全策略：允许 sharp（next@16 间接依赖）运行原生构建脚本
onlyBuiltDependencies[]=sharp
